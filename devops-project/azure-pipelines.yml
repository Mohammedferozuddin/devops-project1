trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'DockerHub'
    repository: 'yourdockerhubusername/devops-web'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
    tags: latest
